export class Sync {
  // keeps track of server state
  // keeps track of local state
  // sends updates to the bus whenever they are received from the server
  // sends updates to the server periodically (e.g. 1 per second when there are any)
}
